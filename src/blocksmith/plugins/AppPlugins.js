/**
 * Extension point for downstream apps to mount additional Lexical plugins
 * inside Blocksmith. Shadow this module at the app level to override.
 */
export default function AppPlugins() {
  return null;
}
